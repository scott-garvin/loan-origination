{
  "name": "loan-origination",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Origin — a loan origination & underwriting pipeline demo (Vue 3 + Vite + TypeScript)",
  "scripts": {
    "dev": "vite",
    "typecheck": "vue-tsc --noEmit",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --port 4173"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.6.2",
    "vite": "^6.0.7",
    "vue-tsc": "^2.1.10"
  }
}
